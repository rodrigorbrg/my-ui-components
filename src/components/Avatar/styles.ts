import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    height: 60,
    width: 60,
    marginVertical: 8,
    marginHorizontal: 1,
  },
  avatar: {
    borderRadius: 25,
    borderWidth: 1,
    justifyContent: 'center',
  },
  label: {
    textAlign: 'center',
    fontSize: 12,
  },
});
