import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexGrow: 0,
    flexDirection: 'row',
    marginVertical: 16,
  },
  contentContainer: {
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'center',
    height: 80,
    flexGrow: 1,
  },
  text: {
    fontSize: 14,
    marginTop: 8,
  },
});
