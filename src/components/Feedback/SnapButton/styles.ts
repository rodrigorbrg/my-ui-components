import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 2,
    height: 40,
    justifyContent: 'center',
    marginEnd: 8,
    width: 40,
  },
  image: {
    height: 24,
    width: 24,
  },
  camera: {
    position: 'absolute',
    bottom: 8,
    right: 8,
  },
  trash: {
    position: 'absolute',
    bottom: 1,
    right: 1,
  },
});
