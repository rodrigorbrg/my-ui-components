import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    borderWidth: 1,
    borderRadius: 4,
    justifyContent: 'space-between',
    flexDirection: 'row',
    // paddingHorizontal: 8,
  },
  text: {
    fontSize: 14,
    textAlign: 'left',
    flex: 1,
    padding: 12,
    marginHorizontal: 8,
  },
});
